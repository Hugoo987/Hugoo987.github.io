# 旅遊規劃 App

這是一個純前端的旅遊規劃工具，提供：

- 明確的每日行程時間表
- 可切換不同旅遊城市
- Leaflet + OpenStreetMap 互動地圖
- 每日景點標記與路線連線

## 本機執行

直接開啟 `index.html` 即可，或用簡單靜態伺服器：

```bash
python3 -m http.server 8000
```

再開啟 `http://localhost:8000`。
